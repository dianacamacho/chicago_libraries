require 'spec_helper'

describe ChicagoLibraries do
  it 'has a version number' do
    expect(ChicagoLibraries::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
